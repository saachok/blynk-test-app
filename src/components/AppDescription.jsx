import styles from '../styles/AppDescriptions.module.css';

const AppDescription = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>DAYRY APP</h1>
      <div className={styles.description}>Comment with no sense</div>
    </div>
  );
};

export default AppDescription;
